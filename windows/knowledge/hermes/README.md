# Hermes Event Log

`events.jsonl` is an append-only stream of notable lab events. Each line is one JSON object.

Use it for:

- agent handoff
- PR/Linear update source material
- reconstructing the exact work order after interruptions
