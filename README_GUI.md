# 🎨 RPI PXE Manager - GUI Edition

<div align="center">

![Version](https://img.shields.io/badge/version-2.4.0_GUI-blue.svg)
![Python](https://img.shields.io/badge/python-3.8+-yellow.svg)
![GUI](https://img.shields.io/badge/GUI-CustomTkinter-green.svg)

**현대적이고 사용자 친화적인 그래픽 인터페이스**

</div>

## 🎯 개요

RPI PXE Manager의 GUI 버전입니다. 터미널 대신 현대적인 그래픽 인터페이스로 라즈베리파이 네트워크 부팅을 관리할 수 있습니다.

## ✨ 주요 기능

### 📊 대시보드
- **실시간 시스템 모니터링**: CPU, 메모리, 디스크 사용률을 프로그레스 바로 시각화
- **네트워크 상태**: 인터페이스, IP 주소, 넷마스크 정보 표시
- **서비스 상태**: dnsmasq, NFS 서버의 실시간 상태 확인
- **클라이언트 요약**: 등록된 클라이언트 수 통계

### 🖥️ 클라이언트 관리
- **직관적인 테이블 뷰**: 모든 클라이언트를 한눈에 확인
- **온라인 상태 표시**: 실시간 ping 체크로 온라인/오프라인 상태 확인 (🟢/⚫)
- **간편한 추가/편집**: 다이얼로그를 통한 쉬운 클라이언트 관리
- **MAC 주소 자동완성**: 라즈베리파이 MAC 주소 자동 완성 지원
- **IP 자동 할당**: 사용 가능한 IP를 자동으로 제안
- **SD 카드 복사**: SD 카드에서 NFS 루트로 시스템 복사 (5-10분)
- **원클릭 삭제**: 확인 후 간단히 클라이언트 제거

### ⚙️ 서버 설정
- **설정 편집**: 모든 서버 설정을 GUI에서 편집
- **즉시 저장**: 변경사항을 바로 저장

### 🚀 서비스 관리
- **서비스 제어**: 각 서비스를 시작/중지/재시작
- **상태 표시**: 각 서비스의 실행 상태를 시각적으로 표시
- **원클릭 제어**: 버튼 하나로 서비스 제어

### 📝 로그 뷰어
- **실시간 로그**: journalctl 로그를 GUI에서 확인
- **서비스별 필터링**: dnsmasq, NFS 등 서비스별로 로그 확인
- **새로고침**: 최신 로그 업데이트

### 🔧 초기 설정 마법사
- **자동 설정**: 필요한 모든 설정을 자동으로 구성
- **진행 상황 표시**: 설정 과정을 실시간으로 확인

## 🚀 실행 방법

### 방법 1: 실행 스크립트 사용 (권장)
```bash
cd rpi-pxe-manager
./run_gui.sh
```

### 방법 2: Python으로 직접 실행
```bash
cd rpi-pxe-manager
python3 pxe_gui.py
```

### 방법 3: 직접 실행 (자동 sudo 인증)
```bash
cd rpi-pxe-manager
python3 pxe_gui.py
# sudo 비밀번호 자동 입력으로 모든 기능 사용 가능
```

## 📦 자동 패키지 설치

프로그램을 처음 실행하면 필요한 패키지들이 자동으로 설치됩니다:
- `customtkinter` - 현대적인 GUI 프레임워크
- `psutil` - 시스템 정보 수집
- `netifaces` - 네트워크 인터페이스 정보
- `pillow` - 이미지 처리

## 🎨 UI 특징

### 다크/라이트 테마
- **다크 모드**: 눈의 피로를 줄이는 어두운 테마 (기본값)
- **라이트 모드**: 밝은 환경에 적합한 밝은 테마
- **쉬운 전환**: 사이드바의 스위치로 즉시 전환

### 반응형 디자인
- **최소 크기**: 1200x700
- **권장 크기**: 1400x900
- **자동 조절**: 창 크기에 따라 레이아웃 자동 조정

### 현대적인 디자인
- **카드형 레이아웃**: 정보를 카드 단위로 구분하여 가독성 향상
- **부드러운 애니메이션**: 버튼 호버 효과 및 전환 애니메이션
- **색상 코드**: 상태에 따른 직관적인 색상 사용
  - 녹색: 정상/활성
  - 빨간색: 오류/비활성
  - 회색: 중립/대기

### 사용자 경험 (UX)
- **아이콘 사용**: 모든 메뉴와 버튼에 이모지 아이콘 적용
- **툴팁**: 중요한 기능에 대한 힌트 제공
- **확인 다이얼로그**: 중요한 작업 전 확인 요청
- **성공/오류 메시지**: 작업 결과를 명확히 안내

## 🔑 주요 화면

### 1. 대시보드 (📊)
시스템의 전반적인 상태를 한눈에 확인할 수 있습니다.
- 시스템 리소스 사용률 (CPU, 메모리, 디스크)
- 네트워크 설정 정보
- 서비스 실행 상태
- 등록된 클라이언트 수

### 2. 클라이언트 관리 (🖥️)
모든 PXE 클라이언트를 관리합니다.
- 클라이언트 목록 (IP 순 정렬, 온라인 상태 표시)
- 새 클라이언트 추가
- 클라이언트 정보 편집
- 클라이언트 삭제
- SD 카드에서 시스템 복사

### 3. 서버 설정 (⚙️)
서버의 기본 설정을 변경합니다.
- 서버 IP 설정
- DHCP 범위 설정
- 네트워크 인터페이스 선택
- NFS/TFTP 루트 경로 설정

### 4. 서비스 관리 (🚀)
시스템 서비스를 제어합니다.
- dnsmasq (DHCP/DNS/TFTP/PXE)
- nfs-kernel-server (NFS 파일 공유)
- 각 서비스: 시작/중지/재시작

### 5. 로그 확인 (📝)
시스템 로그를 확인합니다.
- 서비스별 로그 필터링
- 최근 100줄 표시
- 실시간 새로고침

### 6. 초기 설정 (🔧)
시스템을 처음 설정할 때 사용합니다.
- 패키지 확인
- 네트워크 설정
- dnsmasq 설정
- NFS 설정

## 💡 사용 팁

### 클라이언트 추가 시
1. **시리얼 번호**: 8자리 16진수 (예: `1234abcd`)
2. **MAC 주소**: 마지막 4자리만 입력 가능 (예: `e3:0f` → 자동완성: `88:a2:9e:1b:e3:0f`)
3. **IP 주소**: 자동으로 추천되는 IP 사용 권장

### SD 카드 복사 시
1. **대상 선택**: 복사할 클라이언트 선택
2. **SD 카드 감지**: 🔍 버튼으로 파티션 확인
3. **파티션 입력**: Boot(보통 /dev/sdb1), Root(보통 /dev/sdb2)
4. **복사 시작**: 5-10분 소요, 백그라운드에서 진행
5. **주의사항**: SD 카드는 라즈베리파이 OS가 설치된 상태여야 함

### 서비스 제어 시
- **자동 인증**: sudo 비밀번호가 자동으로 입력됩니다
- **재시작**: 설정 변경 후 반드시 서비스를 재시작하세요
- **상태 확인**: 대시보드에서 서비스 상태를 실시간으로 확인하세요

### 테마 선택
- **다크 모드**: 장시간 사용 시 눈의 피로 감소
- **라이트 모드**: 밝은 환경에서 더 나은 가독성
- **전환**: 사이드바 하단의 스위치로 즉시 전환 가능

## 🔧 문제 해결

### GUI가 실행되지 않을 때
```bash
# 패키지 수동 설치
pip3 install --user customtkinter psutil netifaces pillow

# 또는 시스템 패키지 사용
sudo apt install python3-tk
```

### 서비스 제어가 작동하지 않을 때
```bash
# sudo 비밀번호가 "1234"가 아닌 경우 pxe_gui.py 수정 필요
# run_sudo() 메서드에서 password = b'1234\n' 부분 변경
```

### 디스플레이가 없는 서버에서 사용
GUI는 디스플레이가 필요합니다. 디스플레이가 없는 서버에서는 다음 방법을 사용하세요:
1. **SSH X11 포워딩**:
   ```bash
   ssh -X user@server
   python3 pxe_gui.py
   ```

2. **VNC 사용**:
   ```bash
   # VNC 서버 설치 및 실행 후 VNC 클라이언트로 접속
   ```

3. **터미널 버전 사용**:
   ```bash
   ./pxe  # 기존 터미널 버전
   ```

## 📋 시스템 요구사항

- **운영체제**: Ubuntu 20.04+ / Debian 10+
- **Python**: 3.8 이상
- **디스플레이**: GUI 실행을 위한 X11 또는 Wayland
- **메모리**: 최소 512MB (GUI 사용 시 1GB 권장)
- **권한**: 일부 기능은 sudo 권한 필요

## 🆚 CLI vs GUI

| 기능 | CLI (pxe) | GUI (pxe_gui.py) |
|------|-----------|------------------|
| 시스템 모니터링 | ✅ | ✅ (실시간 그래프) |
| 클라이언트 관리 | ✅ | ✅ (테이블 뷰) |
| 서비스 관리 | ✅ | ✅ (원클릭) |
| 로그 확인 | ✅ | ✅ (GUI 뷰어) |
| 디스플레이 불필요 | ✅ | ❌ |
| 마우스 사용 | ❌ | ✅ |
| 사용 편의성 | 중간 | 높음 |
| 리소스 사용 | 낮음 | 중간 |

## 📸 스크린샷

GUI를 실행하면 다음과 같은 화면을 볼 수 있습니다:
- 왼쪽: 네비게이션 사이드바
- 오른쪽: 메인 컨텐츠 영역
- 상단: 페이지 제목
- 하단: 스크롤 가능한 컨텐츠

## 🤝 기여하기

GUI 개선 아이디어나 버그 리포트를 환영합니다!

## 📄 라이센스

MIT License - CLI 버전과 동일

---

<div align="center">

**🎨 현대적인 GUI로 더 쉽고 편리하게!**

[CLI 버전 README](README.md) | [메인 프로젝트](https://github.com/minoTrey/rpi-pxe-manager)

</div>
